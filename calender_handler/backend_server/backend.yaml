services:
  - type: web
    name: backend-server
    runtime: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production
